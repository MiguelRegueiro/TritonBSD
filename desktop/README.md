# Triton Desktop

This directory will hold the installable Triton desktop skeleton.

Initial source:

```text
/home/regueiro/regueiro-hyprland origin/freebsd
```

The desktop package should install reusable config into:

```text
/usr/local/share/triton/skel
```

The installer then copies that skeleton into the created user's home directory.

